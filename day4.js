const arr = [1, 2, 3, 4, 5]

// delete (arr[1])

// delete function delete the item but not the refernce
console.log(arr)

const obj1 = { a: 1 }
const obj2 = { a: 1 }

// the object compare by referce not by value

// console.log(obj1 === obj2)

const arr2 = [1, 2, 3, 4, 5]
console.log(arr2 == arr)

// same as array 